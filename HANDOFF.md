# 淘宝运营工作台 · 交接说明

> 本文件用于"换电脑 / 新会话"时快速接手项目。项目根目录：`taobao-workbench`
> 更新时间：2026-08-17（对齐当前真实代码状态）

## 一、这是什么

无恙按照工作台形态搭建的淘宝运营平台，还在完善中：

- 后端：FastAPI（Python），端口 `8000`
- 前端：React + Vite + Ant Design，端口 `5173`，`/api` 自动代理到后端
- 数据：SQLite 单文件 `backend/data/taobao.db`（账号、店铺、礼品单、推广、商品、操作日志全在里面）
- 抓取：内置生意参谋（sycm_cli）与万相台（alimama_cli）两个 MIT 协议组件，通过专用 Chrome 的登录态抓真实数据

## 二、怎么跑起来

### 方式 0：一键启动（本机 + 公网，最快，推荐）

桌面双击 **「TB一键启动」**（即 `D:\demo\TB一键启动.vbs`），全程**无命令行窗口**：

- 静默拉起后端（8000）+ 前端（5173）+ cpolar 公网隧道
- 完成后自动弹窗显示**公网地址**，公司/外部电脑浏览器打开该地址即可访问（登录账号见下）
- 自动检测端口，重复双击不会起重复服务

停止服务：桌面双击 **「TB停止服务」**（即 `D:\demo\TB停止服务.vbs`）。
> ⚠️ 停止会关闭公网隧道；cpolar 免费版重启后**公网地址会变**（变了我重新给即可）。
> 平时合盖/锁屏没关系，只要电脑开机、服务没停，公网就一直有效。

相关文件与日志：

| 项目 | 位置 |
|---|---|
| 启动/停止脚本 | `D:\demo\TB一键启动.vbs`、`D:\demo\TB停止服务.vbs`、`D:\demo\start-all.ps1`、`D:\demo\stop-all.ps1` |
| 服务日志 | `D:\demo\logs\backend.log`、`frontend.log`、`service.log` |
| 隧道日志 / 最新公网地址 | `D:\cpolar\tunnel.log`、`D:\demo\public_url.txt` |
| cpolar 客户端 / 配置 | `D:\cpolar\extracted\cpolar\cpolar.exe`，token 在 `C:\Users\Administrator\.cpolar\cpolar.yml` |
| 前端允许的穿透域名 | `frontend/vite.config.ts` 的 `server.allowedHosts`（换穿透服务时需同步加域名） |

首次使用前需先装好依赖（后端 `pip install -r requirements.txt`，前端 `npm install`）。

### 方式 A：有 Python 3.12 / Node 20+ 的电脑（标准步骤）

```bash
cd taobao-workbench
pip install -r requirements.txt
uvicorn backend.app.main:app --reload
```

```bash
cd taobao-workbench/frontend
npm install
npm run dev
```

浏览器打开 http://127.0.0.1:5173

### 方式 B：用 Codex（推荐，装过 Codex 桌面的电脑都能跑）

新开一个 Codex 会话，直接说：

> 项目在 <本项目所在路径>/taobao-workbench，帮我把它跑起来

Codex 会用自带的 Python/Node 运行时自动搞定。前端依赖如果没装，让 Codex 用 `pnpm install` 或 `npm install` 装一下即可。

## 三、默认账号

| 账号 | 密码 | 角色 |
|---|---|---|
| demo_ops | demo123456 | 超级管理员 |
| wuyang | （你自己设的） | 超级管理员 |
| demo2026 | newpass456 | 普通账号（仅商品管理） |
| opsadmin | （你自己设的） | 普通账号 |

> 如果 `backend/data/taobao.db` 没带过来（或被删了）：服务首次启动会自动重建数据库并写入演示数据
> （4 家演示店铺、礼品单样例、操作日志），**第一个注册的账号自动成为超级管理员**。

## 四、已实现功能速览

### 基础与权限
- 登录 / 注册（用户名需字母开头，花名必填），登录拦截，退出登录
- 注册凭邀请码可直接开通（邀请码在「账号管理 → 邀请码」生成）；无邀请码的注册申请进入「待审核」，管理员在「账号管理 → 待审核」通过后才能登录。首个注册用户自动成为超级管理员，无需邀请码
- 三级角色：超级管理员 / 管理员 / 普通账号
- 账号管理：新增/删除、改角色、禁用/启用、重置密码、模块权限、店铺权限、权限复制、账号有效期、
  登录会话管理（强制下线）、单账号操作日志、搜索筛选
- 登录安全：连续 5 次密码错误锁定 10 分钟；token 7 天有效（过期自动失效）；登录/失败/登出审计日志（含 IP 与设备）
- 系统公告：管理员发布/停用/删除公告，所有账号登录后顶部 banner 可见（账号管理 → 公告）
- 套餐/配额 + 子账号（SaaS）：普通账号 = 主账号，默认可创建 2 个子账号、绑定 3 家店铺；
  超管可在「账号管理 → 配额」针对每个账号单独设置子账号配额与店铺配额（users.sub_account_quota / store_quota）；
  主账号在「我的团队」创建子账号，子账号继承主账号绑定的店铺数据（users.parent_id，查询时自动继承）；
  子账号不能自行绑定店铺；主账号可给子账号重置密码/删除
- 租户概览（SaaS）：账号管理 → 租户概览，查看账号/店铺/绑定关系统计与最近登录（`/api/system/tenant-overview`）
- 礼品单订单号查重：创建/编辑时订单号重复会拦截
- 个人中心：改花名、改密码、换头像
- 统一操作日志：所有模块操作记录（仅管理员可见）

### 店铺管理（三版完整）
- 店铺列表、绑定/编辑/解绑、健康状态、刷新授权、停用/启用、当前店铺切换
- 指标速览（详情抽屉 + 14 天趋势图）、经营提醒、多店对比、自动巡检（每 5 分钟）
- 店铺级权限（超管给普通账号指定可见店铺）、操作日志

### 礼品单（真实功能）
- 列表/新增/发货/完成/退款/删除，支持关键字 + 表头筛选
- 台账字段：关键词、规格、佣金、旺旺号、评论状态、结款状态、下单时间、二维码/图片
- 批量创建/批量状态更新/批量删除、Excel 导出、图片上传
- 注意：当前库里礼品单为 0 条（演示种子仅当仍存在 4 家演示店铺时写入）

### 数据洞察（6 个页面，最重的模块）
- 概览：今日/本周/本月/累计指标，趋势图、按店铺汇总（数据来自生意参谋实时抓取）
- 经营日报：默认昨日数据（对比前天），每日 9:00 自动刷新，导出 PDF（浏览器打印排版）/ 复制文本 / 推送到群机器人
- AI 经营分析：按日期缓存（每个日期只生成一次），切换日期显示对应缓存，点「重新生成」才覆盖
- AI 洞察：14 天数据 AI 归因分析，支持追问聊天
- 时段分析：今日/昨日分时（24 小时）趋势，AI 洞察，小时异常推送（pushplus）
- 商品分析：商品排行（实时/日维度）、单品详情/趋势/推广数据，单品 AI 洞察与追问
- 预警：异常预警规则配置（转化率/销售额/访客等阈值），预警摘要

### 推广管理（全站/关键词/人群/内容营销四场景）
- 推广数据：全站/关键词/人群/内容营销四场景，实时/昨日/近 7 天/近 30 天维度
- 推广计划：计划列表（含备注、标签）、计划商品、关键词报表、计划趋势、计划状态
- AI 洞察：推广数据 AI 归因 + 追问；单计划 AI 洞察 + 追问
- 数据同步：手动同步 / 自动同步（后台每 3 分钟）；导出 Excel
- 万相台抓取复用店铺淘宝登录档案（与生意参谋同一套登录态）

### AI 助手与模型配置
- 模型配置：OpenAI 兼容接口（openai / deepseek / dashscope 百炼 / moonshot / custom），
  多模型列表、默认标记、连接测试
- AI 助手：对话式问数据、写文案（依赖模型配置页填好 API Key）

### 经营日报推送（群机器人 webhook）
- 日报页可配置 webhook（如钉钉/企微群机器人），到点自动推送文本日报
- 推送内容含指标卡 + 分区文本；可手动「立即推送」

### 小时异常推送（pushplus 微信）
- pushplus token 绑定微信，配置小时级规则（较昨日同时段/较上一小时/阈值，含「场景」选项）
- 每小时整点过 5 分自动检查，命中即推微信；可手动测试/立即检查

## 四·五、数据对接（重要）

- 原理：后端内置两个抓取组件（`backend/sycm_cli/` 与 `backend/alimama_cli/`，均 MIT 协议）。
  Windows 上通过 CDP 读取一个**专用 Chrome 窗口**的登录态（不导出 Cookie、不动日常浏览器、不降低浏览器安全设置）。
- 绑定流程：店铺管理 → 某店铺 → 生意参谋 → 「打开浏览器登录」→ 在弹出的 Chrome 窗口用该店铺的淘宝账号登录
  生意参谋 → 登录成功后自动保存为该店铺的登录档案并验证。
- 登录档案存放在本机 `~/.taobao-cli/profiles/store_<店铺id>.json`（**切勿提交到 git**，含长效登录凭证）。
- 每个店铺对应一份档案；多店铺时在同一个专用 Chrome 窗口里切换账号再分别绑定即可。
- 实时数据：今天走生意参谋「实时」接口（`portal/live/new/index/overview/v3.json?dateType=today`），
  历史日期走「数据概览」接口；万相台推广复用同一登录态调用 `one.alimama.com` 接口。
- 自动同步：后端每 3 分钟在后台线程同步所有已绑定店铺（日数据 + 分时 + 推广实时 + 商品实时 + 商品级推广）。
- 同步结果反馈：所有同步接口遍历**全部店铺**，未配置登录档案的店铺显式返回
  `生意参谋未登录（未配置登录档案）`，抓取报错时原样带出原因；前端据此弹绿色（全部成功）或
  红色（列出具体失败原因，最多 3 条）提示，不再出现"提示成功但 0 家同步"的假象
  （统一逻辑在 `frontend/src/lib/sync-feedback.ts`）。
- 常见问题：
  - 「还没有绑定」→ 先点「打开浏览器登录」并完成登录。
  - 「登录已失效」→ 重新点「打开浏览器登录」，在窗口里重新登录。
  - 接口触发安全验证 → 稍等几分钟再试，避免短时间频繁点击同步。
  - 同步报 `curl (7) over proxy 127.0.0.1` → 后端进程继承了 WorkBuddy 会话的代理环境变量
    （HTTP_PROXY/HTTPS_PROXY 指向本机代理端口），`sycm.py`/`alimama.py` 的 `_ENV` 已显式清除代理、
    抓取直连；若改动过这两处环境逻辑需保持代理清除。
- 注意：店铺名称只是系统内备注；同步的是「绑定登录时所用账号」的数据，请确保店铺名称与绑定账号一致。

## 四·六、SaaS 多租户数据隔离（重要）

- **规则**：普通账号只能查看/操作自己绑定的店铺的数据；超管/管理员始终看全部店铺；未绑定任何店铺的账号看不到任何店铺数据。
- **绑定方式（两种）**：
  1. 管理员分配：账号管理 → 某账号 → 「店铺权限」勾选（推荐，平台统一管理）
  2. 账号自助绑定：店铺管理 → 「我的店铺」→ 从平台已接入（已配置登录态）的店铺列表选择绑定，验证通过即生效（免审批），可自助解绑
- **隔离范围**：数据洞察（概览/日报/时段/商品/预警/AI）、推广（数据/计划）、总览 Dashboard、礼品单、AI 助手快照 —— 后端查询统一按账号可见店铺过滤（核心辅助：`backend/app/api/auth.py` 的 `visible_store_ids`，配合各模块 `_store_filter`/`_scope_filter`/`_store_clause`）。
- **同一店铺被多个账号绑定**：共享同一份真实抓取数据（数据只存一份，查询时按权限过滤）。
- **数据抓取/同步**：系统级全局执行（后台每 3 分钟），不做账号隔离。
- 普通账号绕过前端直接改接口参数也无法获取无权店铺数据（服务端强制过滤）。
## 五、后台自动循环（main.py lifespan 启动）

| 任务 | 周期 | 说明 |
|---|---|---|
| 店铺健康巡检 `_inspect_loop` | 5 分钟 | 检查授权与健康状态 |
| 全量同步 `_realtime_sync_loop` | 3 分钟 | 店铺日/分时 + 推广实时分时 + 商品实时 + 商品级推广实时（启动后 30 秒开始） |
| 日报推送 `_report_push_loop` | 每分钟检查 | 到点且已配置 webhook 时推送（默认 9:00） |
| 小时异常推送 `_hourly_push_loop` | 整点过 5 分 | pushplus 微信推送 |
| 数据库备份 `_backup_loop` | 每天 | 在线备份 `taobao.db` 到 `D:\demo\backups`，保留最近 7 份 |
| 数据清理 `_cleanup_loop` | 每天 | 删除超过 90 天的历史数据（日表/日志） |
| 日志轮转 `_log_rotate_loop` | 每天 | 清理超过 7 天的日志文件 |

**状态可观测**（2026-08-17 新增，替换原"静默吞异常"设计）：
- 查询：`GET /api/system/loops`（需登录）→ 每个循环的 `last_run / last_success / last_error / error_count / run_count / last_duration`
- 日志：失败写 `logs/loops.log`（ERROR + 连续失败次数 + 耗时 + 错误摘要）；成功不落盘（DEBUG），真实动作（推送成功）写 INFO
- 告警：连续失败 ≥3 次（3 的倍数）且已配置 pushplus token 时推微信告警；同循环 10 分钟内至多一次
- 数据同步已改为**单点隔离**：5 类数据各自 try/commit，单个失败只影响该类，其余照常提交，整轮错误汇总后以 ERROR 记录
- 排障入口：先看 `logs/loops.log`，再 `GET /api/system/loops` 看最近一次失败时间与错误

## 六、注意事项

- 前端依赖（`frontend/node_modules`）体积大，打包/迁移时**不需要带**，到新环境重新 `npm install` 即可。
- 数据文件 `backend/data/taobao.db` 建议随项目一起备份；密码是加盐哈希存储的，不存明文。
- 修改代码前建议先备份原文件；演示数据都是确定性生成的，删掉数据库即可重置。
- 权限设计：模块权限（能看哪些模块）＋店铺权限（能看哪些店铺），两者叠加生效；
  "账号管理""操作日志"仅管理员/超管可见。
- **占位模块（待开发）**：商品管理 products、客户管理 customers、内容运营 content、
  竞品监控 monitoring、任务中心 tasks、设置 settings。其中 settings 后端未挂载路由（前端有占位页）。
  总览 dashboard 前端已完整，但后端 `/api/dashboard` 仍返回示例数据，尚未接真实统计。
- 模型配置里的 API Key 存数据库明文，勿把 `taobao.db` 提交到公开仓库。

- **公网访问安全（重要）**：cpolar 隧道会把整个系统（含 `backend/data/taobao.db` 里的
  生意参谋/万相台登录凭证）暴露到公网，**公网链接不要外传**；建议在 cpolar 后台给隧道加
  访问密码（basic auth），或至少定期更换。公网地址仅在本机 cpolar 隧道运行时有效。