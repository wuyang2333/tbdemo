# 淘宝运营工作台 · 交接说明

> 本文件用于"换电脑 / 新会话"时快速接手项目。项目根目录：`taobao-workbench`

## 一、这是什么

一个仿工作台形态的淘宝运营平台演示项目：

- 后端：FastAPI（Python），端口 `8000`
- 前端：React + Vite + Ant Design，端口 `5173`，`/api` 自动代理到后端
- 数据：SQLite 单文件 `backend/data/taobao.db`（账号、店铺、礼品单、操作日志全在里面）

## 二、怎么跑起来

### 方式 A：有 Python 3.12 / Node 20+ 的电脑（标准步骤）

```bash
cd taobao-workbench
pip install -r requirements.txt
uvicorn backend.app.main:app --reload
```

```bash
cd taobao-workbench/frontend
npm install
npm run dev
```

浏览器打开 http://127.0.0.1:5173

### 方式 B：用 Codex（推荐，装过 Codex 桌面的电脑都能跑）

新开一个 Codex 会话，直接说：

> 项目在 <本项目所在路径>/taobao-workbench，帮我把它跑起来

Codex 会用自带的 Python/Node 运行时自动搞定（本机没有独立 Python/Node 也没关系）。前端依赖如果没装，让 Codex 用 `pnpm install` 或 `npm install` 装一下即可。

## 三、默认账号

| 账号 | 密码 | 角色 |
|---|---|---|
| demo_ops | demo123456 | 超级管理员 |
| demo2026 | newpass456 | 普通账号（仅商品管理） |
| wuyang | （你自己设的） | 管理员 |
| opsadmin | （你自己设的） | 普通账号 |

> 如果 `backend/data/taobao.db` 没带过来（或被删了）：服务首次启动会自动重建数据库并写入演示数据（4 家店铺、礼品单、操作日志），**第一个注册的账号自动成为超级管理员**。

## 四、已实现功能速览

- 登录 / 注册（用户名需字母开头，花名必填），登录拦截，退出登录
- 三级角色：超级管理员 / 管理员 / 普通账号
- 账号管理：新增账号、改角色、禁用/启用、重置密码、模块权限、店铺权限、删除账号（超管）
- 个人中心：改花名、改密码、换头像
- 店铺管理（三版完整）：
  - 店铺列表、绑定/编辑/解绑、健康状态、刷新授权、停用/启用、当前店铺切换
  - 指标速览（详情抽屉 + 14 天趋势图）、经营提醒、多店对比、自动巡检（每 5 分钟）
  - 店铺级权限（超管给普通账号指定可见店铺）、操作日志
- 礼品单：真实功能（列表/新增/发货/完成/退款/删除），不区分店铺，支持关键字 + 表头筛选
- 统一操作日志：所有模块操作记录（仅管理员可见）
- 数据总览洞察：今日/本周/本月/累计的访客、浏览量、支付金额、订单数、转化率，趋势图、按店铺汇总（数据来自生意参谋实时抓取）
- 生意参谋实时数据：店铺管理里可「打开浏览器登录」绑定每店生意参谋账号，登录后按需/每 30 分钟自动同步今天的实时数据（支付金额/访客/浏览量/支付买家数/支付订单数/转化率）

## 四·五、生意参谋数据对接（重要）

- 原理：后端内置了生意参谋抓取组件（`backend/sycm_cli/`，MIT 协议）。Windows 上它通过 CDP 读取一个**专用 Chrome 窗口**的登录态（不导出 Cookie、不动日常浏览器、不降低浏览器安全设置）。
- 绑定流程：店铺管理 → 某店铺 → 生意参谋 → 「打开浏览器登录」→ 在弹出的 Chrome 窗口用该店铺的淘宝账号登录生意参谋 → 登录成功后自动保存为该店铺的登录档案并验证。
- 登录档案存放在本机 `~/.taobao-cli/profiles/store_<店铺id>.json`（**切勿提交到 git**，含长效登录凭证）。
- 每个店铺对应一份档案；多店铺时在同一个专用 Chrome 窗口里切换账号再分别绑定即可。
- 实时数据：今天的数据走生意参谋「实时」接口（`portal/live/new/index/overview/v3.json?dateType=today`），历史日期走「数据概览」接口。
- 自动同步：后端每 30 分钟在后台线程同步一次所有已绑定店铺的今天数据；也可在店铺管理手动「同步数据」/顶部「同步数据」。
- 常见问题：
  - 「还没有绑定」→ 先点「打开浏览器登录」并完成登录。
  - 「登录已失效」→ 重新点「打开浏览器登录」，在窗口里重新登录。
  - 接口触发安全验证 → 稍等几分钟再试，避免短时间频繁点击同步。
- 注意：店铺名称只是系统内备注；同步的是「绑定登录时所用账号」的生意参谋数据，请确保店铺名称与绑定账号一致。

## 五、注意事项

- 前端依赖（`frontend/node_modules`）体积大，打包/迁移时**不需要带**，到新环境重新 `npm install` 即可。
- 数据文件 `backend/data/taobao.db` 建议随项目一起备份；密码是加盐哈希存储的，不存明文。
- 修改代码前建议先备份原文件；本项目的演示数据都是确定性生成的，删掉数据库即可重置。
- 权限设计：模块权限（能看哪些模块）＋店铺权限（能看哪些店铺），两者叠加生效；"账号管理""操作日志"仅管理员/超管可见。
